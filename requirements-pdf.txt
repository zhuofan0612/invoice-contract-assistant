# PDF and OCR ingestion. The repo ships contracts as Markdown so the pipeline
# is runnable without these; they are needed for real scanned contracts.
pymupdf>=1.24
pdfplumber>=0.11
pytesseract>=0.3.10
Pillow>=10.2
