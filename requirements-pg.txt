# Postgres + pgvector backend. Without these the SQLite store is used and the
# same access filter is applied as a SQL predicate -- correct, but a single
# layer. These are what make the row-level security policy in
# app/store/schema_pg.sql an actual second layer rather than a description.
psycopg[binary]>=3.1
psycopg-pool>=3.2
